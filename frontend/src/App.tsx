import Dashboard from './components/Dashboard';

export default function App() {
  return (
    <div style={{
      minHeight: '100vh',
      maxWidth: 900,
      margin: '0 auto',
      padding: '2rem 1.5rem',
    }}>
      <Dashboard />
    </div>
  );
}